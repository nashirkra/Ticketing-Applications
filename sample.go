package main

// var ctx = context.Background()

func Sample() {
	/* 	client := redis.NewClient(&redis.Options{
	   		Addr:     "localhost:6379",
	   		Password: "",
	   		DB:       0,
	   	})
	   	user := valueObjects.User{}
	   	user.ID = 3
	   	user.Username = "fathimzr"
	   	user.Fullname = "Fathimah ZR"
	   	user.Email = "fathimah@mail.com"
	   	user.Password = "passftahimah"
	   	user.Role = "participant"
	   	// user.EmailVerifiedAt = time.Now()

	   	var userVO = valueObjects.NewUserVO(&user) */
	/*
		// update proc
		res, _ := userVO.GetAllUser(ctx, client)
		fmt.Printf("(result:%+v)", res) */
	/*
		val, err := userVO.UpdateUser(ctx, client)
		if err != nil {
			fmt.Println(err)
		}
		fmt.Println(val) */
	/*
		val, err := userVO.InsertUser(ctx, client)
		if err != nil {
			fmt.Println(err)
		}
		fmt.Println(val) */
}
